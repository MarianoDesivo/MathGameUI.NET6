<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="mathGameUI.PreviousGames"
             Title="PreviousGames"
             BackgroundImageSource="pizarron.jpg">
    <ScrollView>
        <VerticalStackLayout
            Padding="200,100,200,50">
            <CollectionView x:Name="gamesList">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnDefinitions="*,*,*,*,*"
                              RowDefinitions="50">

                            <Label Grid.Column="0" Text="{Binding DatePlayed}" FontSize="12"/>
                            <Label Grid.Column="1" Text="{Binding Type}" FontSize="12"/>
                            <Label Grid.Column="2" Text="{Binding Score}" FontSize="12"/>
                            <Label Grid.Column="3" Text="{Binding Id}" FontSize="12"/>
                            <Button Grid.Column="4" Text="Delete" 
                                    BindingContext="{Binding Id}"
                                    Clicked="OnDelete"/>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>